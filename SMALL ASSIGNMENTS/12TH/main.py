def __init__(self, gas_level):
        float(self.gas_level)  #initial gas_level
gas_level = 0
    def add_gas(self, amount):
        float(self.amount) #adds to gas_level
gas_level += amount
float(amount)       
    def fill_up(self):
        13.0 = float(amount) + gas_level
        self.add_gas(float(amount)) #calls the add_gas method.
        if amount >= 13.0:
            return 0
        else:
            return gas_amount
    #def ___repr___ ():
        #return int(self.)
def main():
    #.fillup
    #print()

if __name__ == "__main__":
    main()

# some tests to check your code, Do Not Post These in Vocareum
from test import testEqual
testEqual( Car(10).fill_up(), 3 )
testEqual( Car(20).fill_up(), 0 )
testEqual( Car(5.5).fill_up(), 7.5 )
testEqual( Car(12.5).fill_up(), 0.5 )
testEqual( Car(13).fill_up(), 0 )
